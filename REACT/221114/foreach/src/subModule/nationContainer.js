export default function NationContainer (e) {
    e.preventDefault();
    e.target.
    
    return (
        <a href="." className={`nationBox nation${i}`} style={{ opacity }}
        onChange = {}
        >{i}</a>
    )
}
