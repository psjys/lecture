import NationContainer from "./nationContainer";

export default function NationContainer() {

    let numArray = [1, 2, 3, 4, 5];

    numArray.forEach(function (v) {
        
    })
    return (
        <div class="nation_container">
            <a href="." className="`nationBox nation`"></a>
        </div>
    );
}