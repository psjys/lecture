export default function HangMan({ word }) {
    // 함수 
    // span태그에 각각의 글자 넣기
    // letters변수명 
    // 대상.split
    // 배열의 특정 조건 맞는 값만 정리해서 새로 정리
    return (
        <>
            <grid word = {word}/>
        </>
    );
}
